import React from "react"
import classes from "./TheCanvas.module.css"

const TheCanvas = () => {
    return (
        <canvas className = {classes.TheCanvas}></canvas>
        )
}

export default TheCanvas;